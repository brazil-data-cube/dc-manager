import { Component } from '@angular/core';

/**
 * Loading component
 * simple static component, visible when loading is true
 */
@Component({
  selector: 'app-loading',
  templateUrl: './loading.component.html'
})
export class LoadingComponent {}
