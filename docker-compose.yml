version: "3"

services:
  datacube-manager:
    image: registry.dpi.inpe.br/brazil-data-cube/dc-manager:1.0.0
    ports:
      - "8080:8080"
    environment:
      - DC_MANAGER_MODE=local
      - DC_MANAGER_ITEM_PREFIX=/cubes
      - DC_MANAGER_ITEM_BASE_URL=https://brazildatacube.dpi.inpe.br
